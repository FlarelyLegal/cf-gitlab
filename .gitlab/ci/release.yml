# ─── Release ─────────────────────────────────────────────────────────────────
# Create synchronized releases on GitLab (self-hosted), gitlab.com, and
# GitHub when a semver tag (v*) is pushed. Release notes are generated by
# git-cliff from Conventional Commit history.
#
# Requires:
#   - git-cliff installed on the runner (npm install -g git-cliff)
#   - release-cli installed on the runner (/usr/local/bin/release-cli)
#   - gitlab_self_hosted_mirror CI/CD variable (GitHub classic PAT with public_repo scope)
#   - gitlab_com_release_token CI/CD variable (gitlab.com PAT with api scope)

# Shared script: generate release notes for the current tag using git-cliff.
# --latest extracts only the notes for the most recent tag.
# --strip header removes the "## [vX.Y.Z] - date" heading (release-cli and
# the GitHub API add their own title).
.extract-changelog: &extract-changelog
  - |
    TAG="${CI_COMMIT_TAG}"
    printf "Generating release notes for %s with git-cliff...\n" "${TAG}"
    git-cliff --latest --strip header > release-notes.md
    if [ ! -s release-notes.md ]; then
      printf "WARNING: git-cliff produced empty notes for %s, using fallback.\n" "${TAG}"
      printf "Release %s" "${TAG}" > release-notes.md
    fi
    printf "Extracted %s bytes of release notes for %s\n" \
      "$(wc -c < release-notes.md | tr -d ' ')" "${TAG}"

# ─── GitLab Release ──────────────────────────────────────────────────────────
# Uses release-cli to create a release on the self-hosted GitLab instance.
release-gitlab:
  stage: release
  variables:
    GIT_DEPTH: 0
    GIT_STRATEGY: clone
  script:
    - *extract-changelog
    - |
      release-cli create \
        --name "${CI_COMMIT_TAG}" \
        --tag-name "${CI_COMMIT_TAG}" \
        --description release-notes.md
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

# ─── gitlab.com Release ─────────────────────────────────────────────────────
# Uses curl + jq to create a matching release on the gitlab.com mirror via
# the REST API. The push mirror syncs the tag; this job creates the release
# object with notes.
release-gitlab-com:
  stage: release
  variables:
    GIT_DEPTH: 0
    GIT_STRATEGY: clone
    GITLAB_COM_PROJECT_ID: "79915657"
  script:
    - *extract-changelog
    - |
      NOTES_JSON=$(jq -Rs '.' release-notes.md)
      RESPONSE=$(curl -sf -X POST \
        -H "PRIVATE-TOKEN: ${gitlab_com_release_token}" \
        -H "Content-Type: application/json" \
        "https://gitlab.com/api/v4/projects/${GITLAB_COM_PROJECT_ID}/releases" \
        -d "{
          \"tag_name\": \"${CI_COMMIT_TAG}\",
          \"name\": \"${CI_COMMIT_TAG}\",
          \"description\": ${NOTES_JSON}
        }")
      RELEASE_URL=$(printf '%s' "${RESPONSE}" | jq -r '._links.self // empty')
      if [ -n "${RELEASE_URL}" ]; then
        printf "gitlab.com release created: %s\n" "${RELEASE_URL}"
      else
        printf "Failed to create gitlab.com release. Response:\n%s\n" "${RESPONSE}"
        exit 1
      fi
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

# ─── GitHub Release ──────────────────────────────────────────────────────────
# Uses curl + jq to create a matching release on GitHub via the REST API.
# Runs after mirror-github has pushed the tag.
release-github:
  stage: release
  variables:
    GIT_DEPTH: 0
    GIT_STRATEGY: clone
  needs:
    - job: mirror-github
      optional: false
  script:
    - *extract-changelog
    - |
      NOTES_JSON=$(jq -Rs '.' release-notes.md)
      RESPONSE=$(curl -sf -X POST \
        -H "Authorization: token ${gitlab_self_hosted_mirror}" \
        -H "Accept: application/vnd.github+json" \
        "https://api.github.com/repos/FlarelyLegal/cf-gitlab/releases" \
        -d "{
          \"tag_name\": \"${CI_COMMIT_TAG}\",
          \"name\": \"${CI_COMMIT_TAG}\",
          \"body\": ${NOTES_JSON},
          \"draft\": false,
          \"prerelease\": false
        }")
      RELEASE_URL=$(printf '%s' "${RESPONSE}" | jq -r '.html_url // empty')
      if [ -n "${RELEASE_URL}" ]; then
        printf "GitHub release created: %s\n" "${RELEASE_URL}"
      else
        printf "Failed to create GitHub release. Response:\n%s\n" "${RESPONSE}"
        exit 1
      fi
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

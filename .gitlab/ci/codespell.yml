# ─── Codespell ──────────────────────────────────────────────────────────────
# Check for common typos in code and documentation.
codespell:
  stage: lint
  script:
    - printf "codespell %s\n" "$(codespell --version)"
    - codespell --skip=".git,node_modules,*.lock,.jj"

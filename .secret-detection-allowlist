# Allowlist for detect-secrets pre-receive hook
# Lines matching these regexes are exempt from secret detection.
# One regex per line. Comments start with #.

# The detect-secrets hook itself contains pattern strings (e.g., "RSA private key:::-----BEGIN...")
# that would trigger detection. Exempt any line containing the ::: pattern separator.
:::

# Default placeholder passwords in .env.example files (not real secrets)
-ChangeMe"
